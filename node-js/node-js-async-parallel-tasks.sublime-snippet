<snippet>
    <content><![CDATA[
var asyncTasks = {
    ${1:taskName}: function ( callback ) {
        ${2:writeTaskCode}
    }
};

async.parallel( asyncTasks, function ( err, data ) {
    if ( err ) {
        // handle error
    }
    // handle data
} );
]]></content>
    <tabTrigger>parallel</tabTrigger>
    <scope>source.js</scope>
    <description>Async Parallel</description>
</snippet>
